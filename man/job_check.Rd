% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jobs.R
\name{job_check}
\alias{job_check}
\title{Soft validation and defaults for job payloads}
\usage{
job_check(job_info, partial = FALSE)
}
\arguments{
\item{job_info}{Named list from the request body (or merge candidate).}

\item{partial}{If \code{TRUE}, only validate fields that are present (PATCH-style).}
}
\value{
A normalized named list (full create) or \code{job_info} (partial).
}
\description{
Requires \code{process} for full creates (not for \code{partial} updates). Does \strong{not}
require \code{title} / \code{description} so existing clients stay compatible. Fills
\code{plan} and \code{log_level} defaults when missing on full creates.
}
\keyword{internal}
